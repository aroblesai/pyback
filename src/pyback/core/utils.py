def add(x: int, y: int) -> int:
    """Add two numbers.

    Perform simple integer addition of two input numbers.

    Args:
        x (int): The first number to add
        y (int): The second number to add

    Returns:
        int: The sum of the two numbers
    """
    return x + y
